<%= form_with(model: job_site, local: true) do |form| %>
  <% if job_site.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(job_site.errors.count, "error") %> prohibited this job_site from being saved:</h2>

      <ul>
      <% job_site.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address, id: :job_site_address %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
