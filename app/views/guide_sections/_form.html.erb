<%= form_with(model: guide_section, local: true) do |form| %>
  <% if guide_section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(guide_section.errors.count, "error") %> prohibited this builder from being saved:</h2>

      <ul>
      <% guide_section.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>

<% end %>
